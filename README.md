# Name That Tweet

Uses sklearn and Twitter API to classify tweets by their author.

Configuration file example:
```sh
config = {
    'oauth_token': "",
    'oauth_secret': "",
    'consumer_key': "",
    'consumer_secret': "",
    'twitter_handles': [],
}
```
